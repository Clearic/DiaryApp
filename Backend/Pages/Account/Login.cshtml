@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
}

<div class="auth-container">
    <div class="auth-form">
        <h1>Log in</h1>

        <form method="post">
            <div asp-validation-summary="All" class="validation-msg"></div>
            <div>
                <input asp-for="Input.UserName" class="input-big" placeholder="@Html.DisplayNameFor(x => x.Input.UserName)" />
                <span asp-validation-for="Input.UserName" class="validation-msg"></span>
            </div>
            <div>
                <input asp-for="Input.Password" class="input-big" placeholder="@Html.DisplayNameFor(x => x.Input.Password)" />
                <span asp-validation-for="Input.Password" class="validation-msg"></span>
            </div>
            <div class="auth-buttons">
                <button type="submit" class="button-big">Log in</button>
                <a asp-page="./Register">Register</a>
            </div>
        </form>
    </div>
</div>
